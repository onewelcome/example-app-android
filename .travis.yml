sudo: true
language: android
jdk: oraclejdk8

before_install:
 - chmod +x gradlew
 - source setupArtifactoryCredentialsForTravisCI.sh && copyEnvVarsToGradleProperties

android:
  components:
    - tools
    - platform-tools
    - build-tools-25.0.3
    - android-25
    - extra-android-m2repository
    - extra-google-m2repository